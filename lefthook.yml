pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck
    whitespace:
      run: git diff --check

pre-push:
  parallel: false
  commands:
    build:
      run: npm run build
